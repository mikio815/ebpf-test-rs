arch: aarch64
vmType: vz
memory: 4GiB
cpus: 4
disk: 20GiB

images:
  - location: https://cloud-images.ubuntu.com/noble/current/noble-server-cloudimg-arm64.img
    arch: aarch64

mounts:
  - location: "~"
    writable: true

# provision:
#  - mode: system
#    script: |
#      apt-get update
#      apt-get install -y \
#        clang llvm make gcc pkg-config libelf-dev libbpf-dev \
#        linux-tools-common linux-tools-$(uname -r) \
#        linux-headers-$(uname -r) build-essential strace git
#      sed -i 's/^GRUB_CMDLINE_LINUX=.*/GRUB_CMDLINE_LINUX="lsm=landlock,lockdown,yama,integrity,apparmor,bpf"/' /etc/default/grub
#      update-grub

containerd:
  system: false
  user: false
